# ============================================================
# GER_SPD_decisions —— 粮食系统 Mission / 决议组
# 策划案：《德国决议组设计》粮食部分（AOR 模组）
#
# 【变量约定】（国家级变量，建议在 GER_now_what / 开局事件里 set_variable 初始化）
#   GER_food_surplus                粮食余量        （0~100+，越高越好）
#   GER_agri_development            农业发展度      （0~100+，参照 TNO 做 buff/debuff，越高越好）
#   GER_resentment_1 ~ GER_resentment_15   各大大区农民反感度（0~100，越低越好）
#                                     · 影响征粮 / 粮食税成功概率
#                                     · 过低(<10) 降 SPD 支持度；过高(>90) 增 SPD 支持度
#                                     · 编号对应见下方【地区分区】15 区对照
#                                     · 需在开局事件 set_variable 初始化为 0
#   GER_SPD_support                 SPD 支持度      （分地 +，合作社 -）
#
# 【基调 = country flag】（二选一互斥，由 GER_land_distribution / GER_cooperative_reform 设置）
#   GER_agri_tone_land_distributed   分地：+SPD 支持度，但征粮/粮食税成功率与收益下降（护田）
#   GER_agri_tone_cooperative        合作社：-SPD 支持度，但征粮/粮食税成功率与收益上升（组织化）
#   三个 mission 通过 has_country_flag 检测基调，输出不同结果
#
# 【地区分区】（定稿 · 按 designed.png 紫线大区，共 15 区）
#   覆盖开局即属 GER 的 66 州；后续 mission 可通过 is_in_state / any_state 按州 ID 分流结算。
#
#   大区 / 州 ID 列表：
#     1  巴伐利亚 Bayern（南部）            : 52 53 54 857 861 863
#     2  符腾堡 Württemberg（西南）         : 837 841 860
#     3  巴登 Baden（西南）                 : 50
#     4  莱茵兰 Rheinland（西部）           : 42 51 850 859 1118 1119
#     5  威斯特法伦 Westfalen（西部）       : 57 854 1115
#     6  下萨克森 Niedersachsen（西北）     : 56 59 853 1108 1112 1116 1117
#     7  汉萨城市 Hansestädte（西北）       : 838 862 866
#     8  石勒苏益格-荷尔斯泰因 S-H（西北） : 58 839 855 1043 1113
#     9  勃兰登堡及波美拉尼亚 B/P（东北）   : 61 62 63 64 68 86 1110
#     10 普鲁士 Preussen/Ostpreußen（远东） : 5 85 188 763 807 864 865
#     11 柏林 Berlin（东北·特区）           : 840
#     12 西里西亚 Schlesien（东部）         : 66 67 762 849 871
#     13 图林根 Thüringen（中部）           : 60 1114
#     14 黑森 Hessen（中西部）              : 55 856 858 1101 1109 1121
#     15 上萨克森 Obersachsen（中部）       : 65 842 870 1111
#
# 【政治派系划分】（策划案 · 用于 mission 有效度修正，编号对应上面 15 区）
#   左翼核心区  ：柏林(11·不用管) 汉堡—石荷(7,8) 萨克森—图林根(15,13) 鲁尔(4,5)
#   右翼核心区  ：巴伐利亚(1) 东普鲁士(10) 波美拉尼亚(9) 汉诺威(6)
#                  → 对应大区 {1, 6, 9, 10}，强制征粮（粮食摊派）有效度降低
#   被萨尔化的莱茵兰（不用管）：4
#   西南魏玛联盟巴登—符腾堡   ：2,3
#   摇摆区      ：威斯特法伦(5) 威悉—埃姆斯(6) 西里西亚(12) 勃兰登堡(9) 梅克伦堡(9) 黑森(14)
#   注：mission 粒度为大区级，州级派系中若同一大区横跨不同派系（如 6 含汉诺威+威悉埃姆斯、
#       9 含波美拉尼亚+勃兰登堡+梅克伦堡），按右翼核心区归属整体处理。
# ============================================================

GER_SPD_decisions = {

    # =====================================================
    # 地区粮食工作 mission（15 个大区，各一个）
    #   大区事件触发激活后，按 timeout_effect 轮转结算。
    #   结算逻辑统一在 AOR_GER_region_grain_work（scripted effect），
    #   调用前 set_variable GER_region_work_id = 大区编号。
    #
    #   编号 / 大区对照：
    #     1 巴伐利亚  2 符腾堡  3 巴登  4 莱茵兰  5 威斯特法伦
    #     6 下萨克森  7 汉萨城市  8 石勒苏益格-荷尔斯泰因
    #     9 勃兰登堡及波美拉尼亚  10 普鲁士  11 柏林  12 西里西亚
    #     13 图林根  14 黑森  15 上萨克森
    # =====================================================

    # 1 巴伐利亚
    GER_region_bayern_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_1 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 1 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_bayern_mission
        }
    }

    # 2 符腾堡
    GER_region_wurttemberg_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_2 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 2 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_wurttemberg_mission
        }
    }

    # 3 巴登
    GER_region_baden_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_3 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 3 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_baden_mission
        }
    }

    # 4 莱茵兰
    GER_region_rheinland_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_4 > 95 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 4 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_rheinland_mission
        }
    }

    # 5 威斯特法伦
    GER_region_westfalen_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_5 > 95 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 5 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_westfalen_mission
        }
    }

    # 6 下萨克森
    GER_region_niedersachsen_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_6 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 6 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_niedersachsen_mission
        }
    }

    # 7 汉萨城市
    GER_region_hansa_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_7 > 95 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 7 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_hansa_mission
        }
    }

    # 8 石勒苏益格-荷尔斯泰因
    GER_region_schleswig_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_8 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 8 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_schleswig_mission
        }
    }

    # 9 勃兰登堡及波美拉尼亚
    GER_region_brandenburg_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_9 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 9 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_brandenburg_mission
        }
    }

    # 10 普鲁士
    GER_region_preussen_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_10 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 10 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_preussen_mission
        }
    }

    # 11 柏林
    GER_region_berlin_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_11 > 95 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 11 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_berlin_mission
        }
    }

    # 12 西里西亚
    GER_region_schlesien_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_12 > 95 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 12 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_schlesien_mission
        }
    }

    # 13 图林根
    GER_region_thuringen_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_13 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 13 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_thuringen_mission
        }
    }

    # 14 黑森
    GER_region_hessen_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_14 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 14 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_hessen_mission
        }
    }

    # 15 上萨克森
    GER_region_obersachsen_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_15 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 15 }
            AOR_GER_region_grain_work = yes
            activate_mission = GER_region_obersachsen_mission
        }
    }

    # =====================================================
    # 组织城乡交换 mission（15 个大区，各一个）
    #   效果：扣除余粮、增加农业发展度、降低本区不满
    #   结算逻辑见 AOR_GER_region_urban_rural_exchange
    # =====================================================

    # 1 巴伐利亚
    GER_region_bayern_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 1 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_bayern_exchange_mission
        }
    }

    # 2 符腾堡
    GER_region_wurttemberg_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 2 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_wurttemberg_exchange_mission
        }
    }

    # 3 巴登
    GER_region_baden_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 3 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_baden_exchange_mission
        }
    }

    # 4 莱茵兰
    GER_region_rheinland_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 4 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_rheinland_exchange_mission
        }
    }

    # 5 威斯特法伦
    GER_region_westfalen_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 5 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_westfalen_exchange_mission
        }
    }

    # 6 下萨克森
    GER_region_niedersachsen_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 6 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_niedersachsen_exchange_mission
        }
    }

    # 7 汉萨城市
    GER_region_hansa_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 7 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_hansa_exchange_mission
        }
    }

    # 8 石勒苏益格-荷尔斯泰因
    GER_region_schleswig_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 8 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_schleswig_exchange_mission
        }
    }

    # 9 勃兰登堡及波美拉尼亚
    GER_region_brandenburg_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 9 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_brandenburg_exchange_mission
        }
    }

    # 10 普鲁士
    GER_region_preussen_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 10 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_preussen_exchange_mission
        }
    }

    # 11 柏林
    GER_region_berlin_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 11 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_berlin_exchange_mission
        }
    }

    # 12 西里西亚
    GER_region_schlesien_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 12 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_schlesien_exchange_mission
        }
    }

    # 13 图林根
    GER_region_thuringen_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 13 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_thuringen_exchange_mission
        }
    }

    # 14 黑森
    GER_region_hessen_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 14 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_hessen_exchange_mission
        }
    }

    # 15 上萨克森
    GER_region_obersachsen_exchange_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_food_surplus < 4 } }
        is_good = yes
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 15 }
            AOR_GER_region_urban_rural_exchange = yes
            activate_mission = GER_region_obersachsen_exchange_mission
        }
    }

    # =====================================================
    # 强制征粮 mission（15 个大区，各一个）
    #   效果：获得余粮，但大量损害农业、激起大量不满
    #   结算逻辑见 AOR_GER_region_force_requisition
    # =====================================================

    # 1 巴伐利亚
    GER_region_bayern_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_1 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 1 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_bayern_requisition_mission
        }
    }

    # 2 符腾堡
    GER_region_wurttemberg_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_2 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 2 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_wurttemberg_requisition_mission
        }
    }

    # 3 巴登
    GER_region_baden_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_3 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 3 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_baden_requisition_mission
        }
    }

    # 4 莱茵兰
    GER_region_rheinland_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_4 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 4 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_rheinland_requisition_mission
        }
    }

    # 5 威斯特法伦
    GER_region_westfalen_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_5 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 5 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_westfalen_requisition_mission
        }
    }

    # 6 下萨克森
    GER_region_niedersachsen_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_6 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 6 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_niedersachsen_requisition_mission
        }
    }

    # 7 汉萨城市
    GER_region_hansa_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_7 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 7 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_hansa_requisition_mission
        }
    }

    # 8 石勒苏益格-荷尔斯泰因
    GER_region_schleswig_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_8 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 8 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_schleswig_requisition_mission
        }
    }

    # 9 勃兰登堡及波美拉尼亚
    GER_region_brandenburg_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_9 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 9 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_brandenburg_requisition_mission
        }
    }

    # 10 普鲁士
    GER_region_preussen_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_10 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 10 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_preussen_requisition_mission
        }
    }

    # 11 柏林
    GER_region_berlin_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_11 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 11 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_berlin_requisition_mission
        }
    }

    # 12 西里西亚
    GER_region_schlesien_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_12 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 12 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_schlesien_requisition_mission
        }
    }

    # 13 图林根
    GER_region_thuringen_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_13 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 13 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_thuringen_requisition_mission
        }
    }

    # 14 黑森
    GER_region_hessen_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_14 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 14 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_hessen_requisition_mission
        }
    }

    # 15 上萨克森
    GER_region_obersachsen_requisition_mission = {
        allowed = { tag = GER }
        available = { always = no }
        activation = { always = no }
        cancel_trigger = { check_variable = { GER_resentment_15 > 95 } }
        is_good = no
        days_mission_timeout = 30
        selectable_mission = no
        timeout_effect = {
            set_variable = { GER_region_work_id = 15 }
            AOR_GER_region_force_requisition = yes
            activate_mission = GER_region_obersachsen_requisition_mission
        }
    }

    # =====================================================
    # 4) 通用基调：分地  GER_land_distribution
    #    所有地区通用 · 二选一（与合作社互斥）
    #    效果：+SPD 支持度，但 征粮/粮食税 更难看（flag 被 mission 检测）
    #    设置 flag：GER_agri_tone_land_distributed
    # =====================================================
    GER_land_distribution = {
        icon = generic_political_actions
        allowed = { tag = GER }
        available = {
            has_political_power > 25
            NOT = { has_country_flag = GER_agri_tone_cooperative }
        }
        visible = {
            NOT = { has_country_flag = GER_agri_tone_land_distributed }
        }
        cost = 25
        days_remove = 15
        fire_only_once = yes
        complete_effect = {
            # 进行中：丈量土地、分配地契（占位）
        }
        remove_effect = {
            add_political_power = -10
            # 分地 → 农民拥护 SPD（支持度+），但护田让征粮/税变难
            add_to_variable = { GER_SPD_support = 5 }
            set_country_flag = GER_agri_tone_land_distributed
            clr_country_flag = GER_agri_tone_cooperative
        }
        ai_will_do = { factor = 1 }
    }

    # =====================================================
    # 5) 通用基调：原地改组为合作社  GER_cooperative_reform
    #    所有地区通用 · 二选一（与分地互斥）
    #    效果：-SPD 支持度，但 征粮/粮食税 更顺利、收上来的粮食更多
    #    设置 flag：GER_agri_tone_cooperative
    # =====================================================
    GER_cooperative_reform = {
        icon = generic_political_actions
        allowed = { tag = GER }
        available = {
            has_political_power > 25
            NOT = { has_country_flag = GER_agri_tone_land_distributed }
        }
        visible = {
            NOT = { has_country_flag = GER_agri_tone_cooperative }
        }
        cost = 25
        days_remove = 15
        fire_only_once = yes
        complete_effect = {
            # 进行中：清产核资、组织合作社（占位）
        }
        remove_effect = {
            add_political_power = -10
            # 合作社 → 部分农民观望（支持度小幅下滑），但组织化征收更顺、粮食更足
            # 注：SPD 的农民基本盘不多，支持度损失不宜过大
            add_to_variable = { GER_SPD_support = -3 }
            add_to_variable = { GER_food_surplus = 5 }
            set_country_flag = GER_agri_tone_cooperative
            clr_country_flag = GER_agri_tone_land_distributed
            # TODO: 合作社动员事件
        }
        ai_will_do = { factor = 1 }
    }

}
